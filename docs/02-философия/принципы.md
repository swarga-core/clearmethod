# Фундаментальные принципы

ClearMethod стоит на семи фундаментальных принципах. Это не правила, которым нужно слепо следовать — это фундамент, на котором строится вся методология. Понимание этих принципов позволяет не просто использовать ClearMethod, но адаптировать его под свои нужды.

## Зачем нужны принципы?

Большинство разработчиков работают с AI хаотично:
- Вставляют код в чат и надеются на лучшее
- Теряют контекст после перезапуска
- Не могут воспроизвести результаты
- Накапливают технический долг от "быстрых решений" AI

**Принципы ClearMethod превращают хаос в систему.** Они объясняют не "как нажимать кнопки", а "почему мы делаем именно так". Это философия подхода к работе с AI в разработке.

### От хаоса к структуре

```
Без принципов:                     С принципами:
"AI, сделай фичу" →               Task → Workflow → Stages
  Что-то непонятное                 Понятный процесс
  Непредсказуемый результат         Воспроизводимый результат
  История потеряна                  Полный audit trail
```

Принципы — это не бюрократия. Это **профессионализм**.

---

## Семь фундаментальных принципов

### 1. От Диалога к Протоколу (Declarative-First)

> **"AI работает не через промпты, а как исполнитель структурированных инструкций"**

#### Суть принципа

Традиционная работа с AI — это диалог: "Напиши функцию", "Исправь баг", "Добавь тесты". Каждый раз по-новому, каждый раз с риском непонимания.

**ClearMethod меняет парадигму:** AI работает не через свободный диалог, а через **декларативные инструкции** (CML). Вместо "как сделать" мы описываем "что должно быть".

Это как разница между:
- **Диалог:** "Поезжай по главной, потом налево на светофоре..."
- **Протокол:** GPS показывает маршрут, ты следуешь по нему

#### Три сдвига

**1. От промптов к спецификациям**

Вместо:
```
"Сделай авторизацию через JWT"
```

Пишем:
```yaml
# specs.md
## Требование: Авторизация пользователей
Система ДОЛЖНА выдавать JWT токен при успешной авторизации.

Сценарий: Успешный вход
- КОГДА пользователь вводит валидные credentials
- ТОГДА система возвращает JWT токен
- И токен действителен 24 часа
```

**2. От истории чата к документированному состоянию**

Вместо:
```
[Диалог на 50 сообщений потерян при закрытии чата]
```

Имеем:
```yaml
# .cm/tasks/TASK-001/task.yml
state: implementing
workflow: feature
history:
  - stage: analyzing, completed: 2024-11-26 10:00
  - stage: designing, completed: 2024-11-26 11:00
  - stage: planning, completed: 2024-11-26 12:00
artifacts:
  - specs.md
  - design.md
  - plan.md
```

**3. От импровизации к воспроизводимости**

Каждый workflow описан в YAML:
```yaml
# workflows/feature/workflow.yml
states:
  - creating → analyzing → designing → 
    planning → implementing → verifying → completing

transitions:
  - from: planning
    to: implementing
    guard: plan_approved == true
```

Любой агент (или человек) может подхватить работу с любого момента.

#### Как это работает в CM

**CML (ClearMethod Language)** — декларативный DSL на основе YAML:
- Workflow описывает процесс
- Task хранит состояние
- Events связывают компоненты

**Результат:** Работа структурирована, воспроизводима, понятна.

#### Связь с другими принципами

- **Прозрачность Состояния:** Протокол делает состояние явным
- **Проверяй, Не Верь:** Декларативность позволяет валидировать
- **Человек Управляет:** Протокол можно проверить и изменить

---

### 2. Проверяй, Не Верь (Reality-First)

> **"Верификация превыше всего. AI может галлюцинировать — это не баг, это его природа"**

#### Суть принципа

AI иногда выдумывает несуществующие функции, API, библиотеки. Это не значит, что AI плохой — это **особенность технологии**. 

**Проверяй, Не Верь** означает: доверие строится на верификации, а не на слепой вере.

#### Что проверяем

**1. Реальность кодовой базы**

```
AI: "Используй существующую функцию validateUser() из utils"
✅ Действие: grep utils для проверки
❌ Ошибка: Просто добавить вызов без проверки
```

**2. Качество результата**

После каждого stage:
```yaml
# SBD workflow
implementing → verifying:
  quality_gates:
    - linter: must pass
    - tests: must pass
    - coverage: ≥80%
```

Если gate проваливается — переход блокируется.

**3. Соответствие спецификациям**

```
Specs: "Токен действителен 24 часа"
Code: expiresIn: '1h'
❌ Несоответствие обнаружено → исправить
```

#### Reality checks в действии

**Пример 1: Проверка существования**

```
Разработчик: "Добавь интеграцию с EmailService"

AI: "Проверяю наличие EmailService..."
    [codebase_search "EmailService"]
    
AI: "✓ EmailService найден в services/email.service.ts
     Методы: sendEmail(), sendBulk(), getTemplate()
     Могу использовать?"

Разработчик: "Да"

Результат: Корректная интеграция с реальным сервисом
```

**Пример 2: Quality gates блокирует проблему**

```
Stage: implementing → verifying

QA_GATE.check():
  ✓ Linter: passed
  ✗ Tests: 2 failed
  ✓ Coverage: 85%

Transition: BLOCKED
AI: "Тесты провалились. Исправляю..."
```

#### Культура проверки

**Автоматизированные проверки:**
- Linter на каждом stage
- Тесты перед завершением
- Reality checks для предположений AI

**Человеческие проверки:**
- Design review перед implementing
- Code review перед merging
- Acceptance criteria перед completing

#### Как это работает в CM

**qa-gates package:**
```yaml
checks:
  linter:
    command: npm run lint
    blocking: true
  tests:
    command: npm test
    blocking: true
    coverage_threshold: 80
```

**Результат:** Проблемы обнаруживаются рано, качество контролируется.

---

### 3. Контекст — Это Валюта (Context-Efficient)

> **"Не больше информации, а правильная информация. Context window — ограниченный ресурс"**

#### Суть принципа

Context window AI — это как оперативная память: ограничена и дорога. Загрузка всего проекта:
- Замедляет работу
- Увеличивает стоимость
- Создает шум
- Приводит к потере фокуса

**Context-Efficient** означает: стратегический подход к загрузке контекста.

#### Стратегия контекста в CM

**Priming (priming.yml):**
```yaml
# .cm/priming.yml
core:
  - packages/core-concept/README.md
  - docs/architecture.md

stage_specific:
  analyzing:
    - docs/workflows/sbd/analyzing-guide.md
  implementing:
    - docs/workflows/sbd/coding-standards.md
    
task_context:
  - tasks/{{task_id}}/task.yml
  - tasks/{{task_id}}/specs.md
  - tasks/{{task_id}}/design.md
```

**Just-In-Time loading:**

```
Stage: analyzing
  Load: core + analyzing-guide + task specs (500 tokens)

Stage: implementing  
  Load: core + coding-standards + design + plan (1000 tokens)
  
По необходимости:
  "Нужен AuthService" → load только AuthService interface (100 tokens)
```

#### Три уровня контекста

**Level 1: Minimal (всегда)**
- Конфигурация проекта
- Текущая задача
- Stage-specific guide

**Level 2: Focused (по требованию)**
- Релевантные модули
- Зависимости
- Интерфейсы

**Level 3: Deep (редко)**
- Полный код модуля
- История изменений
- Related tasks

#### Антипаттерн: Context dump

```
❌ Плохо:
Загружает 10 файлов "на всякий случай"
→ 20K tokens
→ AI теряется в деталях
→ Медленно, дорого

✅ Хорошо:
Загружает минимум для старта
→ 500 tokens
→ Добавляет по необходимости
→ Быстро, дешево, фокусированно
```

#### Multi-stage = Fresh context

SBD workflow разделен на stages не случайно:

```
Session 1 (Analyzing): 
  Context: specs + architecture
  →  Завершен → Close context

Session 2 (Designing):
  FRESH context: analysis.md + architecture
  → Завершен → Close context

Session 3 (Planning):
  FRESH context: design.md
  → Завершен → Close context
```

Каждый stage = чистое контекстное окно = стабильное качество.

#### Будущее: smart-context

Планируемый пакет:
- Автоматический relevance scoring
- Динамическая загрузка
- Token budget management
- Context compression

**Результат:** Эффективная работа с контекстом, низкая стоимость, высокое качество.

---

### 4. Человек Управляет (Human-in-Control)

> **"AI предлагает варианты. Человек принимает решения. Ответственность всегда за разработчиком"**

#### Суть принципа

AI — мощный инструмент, но он не должен принимать критические решения самостоятельно. **Human-in-Control** означает: финальная ответственность и контроль остаются за человеком.

#### Три уровня контроля

**1. Информирование (AI объясняет)**

```
AI: "Предлагаю создать новый сервис UserNotificationService
     
     ЧТО: Сервис для отправки уведомлений
     ПОЧЕМУ: Текущий код размазан по 3 модулям
     КАК ВЛИЯЕТ: Потребуется рефакторинг 5 файлов
     АЛЬТЕРНАТИВЫ: Можно оставить как есть, но будет сложнее поддерживать
     
     Продолжаем?"
```

Человек принимает решение на основе полной информации.

**2. Подтверждение (Approval gates)**

```yaml
# SBD workflow
designing → planning:
  gate:
    type: human_review
    question: "Design утверждён?"
    
planning → implementing:
  gate:
    type: human_review
    question: "План понятен и готов к реализации?"
```

Критические переходы требуют explicit approval.

**3. Блокировка (Опасные операции запрещены)**

```yaml
# Safety levels
Level 0 (read-only): Без ограничений
Level 1 (safe): Автоматически
Level 2 (changes): С подтверждением
Level 3 (critical): Явное подтверждение + review
Level 4 (dangerous): Запрещено AI
```

**Примеры:**

```
Level 1: Форматирование кода → автоматически
Level 2: Рефакторинг → с подтверждением
Level 3: Изменение API → review обязателен
Level 4: Удаление данных → только вручную
```

#### Баланс автоматизации и контроля

**Автоматизируем безопасное:**
- Генерация тестов
- Форматирование кода
- Обновление документации при изменениях
- Поиск информации
- Линтинг

**Контролируем критическое:**
- Архитектурные решения
- Изменения публичных API
- Миграции БД
- Конфигурация безопасности
- Deployment в production

#### Примеры в действии

**Пример 1: Архитектурное решение**

```
Разработчик: "Нужна real-time связь для notifications"

AI: "Анализирую варианты:
     
     1. WebSockets (ws)
        + Полный контроль
        - Требует инфраструктуру для scale
        
     2. Socket.io
        + Простая интеграция
        + Auto-reconnect
        - Дополнительная зависимость
        
     3. Server-Sent Events
        + Стандарт браузера
        - Только one-way
     
     Учитывая, что у вас уже Redis и нужен bi-directional,
     рекомендую Socket.io. Но это архитектурное решение.
     
     Какой подход выбираете?"

Разработчик: [Принимает решение]
```

**Пример 2: Quality gate блокирует**

```
Stage: implementing → verifying

QA_GATE: ✗ Tests failed

AI: "Не могу перейти к verifying. Исправить тесты?"

Разработчик: "Да" или "Нет, проблема в самих тестах"
```

**Пример 3: Опасная операция**

```
Разработчик случайно: "Удали старые логи из БД"

AI: "❌ Удаление данных запрещено для AI.
     
     Могу создать SQL скрипт с SELECT для preview:
     SELECT * FROM logs WHERE created_at < DATE_SUB(NOW(), INTERVAL 30 DAY);
     
     Но DELETE должен выполнить человек после проверки."
```

#### Как это работает в CM

**Quality gates:** Автоматически блокируют проблемные переходы

**Approval gates:** Требуют явного подтверждения человека

**Safety levels:** Определяют что AI может делать сам

**Результат:** AI ускоряет работу, но не принимает критические решения.

---

### 5. Прозрачность Состояния (Stateful Transparency)

> **"Всегда понятно: где мы сейчас, как сюда попали, что дальше"**

#### Суть принципа

В хаотичной работе с AI главный вопрос: "Где мы вообще?" История потеряна в чате, непонятно что сделано, что осталось.

**Stateful Transparency** означает: состояние процесса всегда видимо, документировано и понятно.

#### Task — точка правды

```yaml
# .cm/tasks/TASK-001/task.yml
id: TASK-001
state: implementing
workflow: feature
created_at: 2024-11-26T10:00:00Z

context:
  description: "Add two-factor authentication"
  priority: high
  
history:
  - stage: creating
    started: 2024-11-26T10:00:00Z
    completed: 2024-11-26T10:15:00Z
    
  - stage: analyzing
    started: 2024-11-26T10:15:00Z
    completed: 2024-11-26T11:30:00Z
    notes: "Requirements clarified, TOTP chosen"
    
  - stage: designing
    started: 2024-11-26T11:30:00Z
    completed: 2024-11-26T13:00:00Z
    notes: "Architecture designed, OTP table planned"
    
  - stage: planning
    started: 2024-11-26T13:00:00Z
    completed: 2024-11-26T14:00:00Z
    
  - stage: implementing
    started: 2024-11-26T14:00:00Z
    # В процессе...

artifacts:
  - specs.md
  - analysis.md
  - design.md
  - plan.md

metadata:
  assignee: alice@company.com
  labels: [security, authentication]
```

Открыв эту задачу, любой человек (или AI) понимает:
- **Где мы:** implementing stage
- **Что сделано:** 4 stage завершены
- **Что дальше:** verifying, completing
- **Детали:** Артефакты в папке задачи

#### Полный audit trail

```markdown
# .cm/tasks/TASK-001/log.md

## 2024-11-26 10:00 | TASK CREATED
Description: Add two-factor authentication
Workflow: feature (7 stages)

## 2024-11-26 10:15 | STAGE: creating → analyzing
Specs completed and approved

## 2024-11-26 11:30 | STAGE: analyzing → designing
Analysis complete:
- Chosen approach: TOTP (Time-based OTP)
- Library: speakeasy
- Storage: new table users_otp

## 2024-11-26 13:00 | STAGE: designing → planning
Design approved:
- Database schema designed
- API endpoints planned
- Security considerations documented

## 2024-11-26 14:00 | STAGE: planning → implementing
Plan approved. Starting implementation...

## 2024-11-26 15:00 | QA GATE CHECK
✓ Linter: passed
✗ Tests: 2 failed
Fixing tests...

## 2024-11-26 15:30 | QA GATE CHECK
✓ Linter: passed
✓ Tests: passed
✓ Coverage: 87%
```

#### Воспроизводимость

**Без прозрачности состояния:**
```
[Разработчик закрыл чат]
[На следующий день]
"Эм... что мы делали? Где остановились?"
[Начинает с нуля или пытается вспомнить]
```

**С прозрачностью состояния:**
```
[Разработчик закрыл чат]
[На следующий день]
cm status TASK-001
→ State: implementing
→ Last action: QA gate passed
→ Next: finish implementation, move to verifying

[Продолжает с точного места]
```

#### Handoff между людьми/агентами

```
Alice (вечер):
  → Завершила designing stage
  → Обновила task.yml: design_approved = true
  → Закоммитила design.md
  
Bob (утро):
  → /cm status TASK-001
  → Видит: design approved, planning stage next
  → /cm next
  → AI загружает design.md и начинает planning
```

Нет зависимости от "истории чата Alice".

#### Канбан-визуализация

```markdown
# .cm/KANBAN.md (generated by file-kanban)

## 📋 To Do
- TASK-005: Payment gateway integration

## 🔄 In Progress  
- TASK-001: Two-factor auth [implementing] [alice]
- TASK-003: API rate limiting [designing] [bob]

## ✅ Done
- TASK-002: User profile page
- TASK-004: Email verification
```

Визуальная прозрачность для команды.

#### Как это работает в CM

**file-task:** Хранит состояние в YAML + Markdown

**Audit trail:** Каждое действие логируется в log.md

**file-kanban:** Автоматически генерирует board

**Результат:** Полная прозрачность процесса, воспроизводимость, командная работа.

---

### 6. Модульная Композиция (Package-Driven)

> **"Собирай нужное, не тащи лишнее. Расширяемость через packages"**

#### Суть принципа

Нет универсального решения для всех. Стартапу не нужны compliance механизмы. Финтех-проекту критичны audit trails. Open-source проекту не нужна интеграция с корпоративным Jira.

**Package-Driven** означает: собери свою конфигурацию ClearMethod из нужных пакетов.

#### Архитектура пакетов

```
ClearMethod
├── Core (минимум)
│   └── Инфраструктура запуска
│
├── Concepts (абстракции)
│   ├── core-concept (TASK, WORKFLOW, AGENT, CONTEXT, EVENT)
│   ├── vcs-concept (VCS)
│   ├── qa-gate-concept (QA_GATE)
│   ├── kanban-concept (KANBAN)
│   └── notification-concept (NOTIFICATION)
│
└── Implementations (конкретика)
    ├── file-task (хранение задач)
    ├── basic-events (события)
    ├── sbd (workflows)
    ├── git-vcs (Git интеграция)
    ├── qa-gates (quality проверки)
    ├── file-kanban (доска)
    └── ... (расширяемо)
```

#### Concepts + Implementations

**Concept (что):**
```yaml
# packages/concepts/vcs-concept/concept.yml
concept: VCS
description: Version control system integration
methods:
  - commit(message, files)
  - push(remote, branch)
  - create_branch(name)
  - create_pr(title, body)
```

**Implementation (как):**
```yaml
# packages/implements/git-vcs/package.yml
name: git-vcs
type: implementation
implements: VCS

provides:
  methods:
    commit: methods/commit.yml
    push: methods/push.yml
    create_branch: methods/create_branch.yml
```

**Результат:** Можно заменить git-vcs на hg-vcs без изменения workflows.

#### Композиция под задачу

**Минимальная конфигурация (solo dev, простой проект):**
```yaml
# .cm/project.yml
packages:
  - core-concept
  - file-task
  - sbd
```

**Полная конфигурация (команда, production):**
```yaml
packages:
  - core-concept
  - file-task
  - sbd
  - git-vcs       # Git automation
  - qa-gates      # Quality checks
  - file-kanban   # Team board
  - basic-events  # Event system
```

**Enterprise конфигурация (compliance, audit):**
```yaml
packages:
  - core-concept
  - jira-task
  - sbd
  - git-vcs
  - qa-gates
  - jira-kanban
  - basic-events
  - smart-context  # Оптимизация контекста
  - auto-docs      # Документация
  - pii-protection # Защита PII
  - audit-trail    # Compliance
```

#### Event-driven интеграция

Пакеты общаются через события:

```yaml
# git-vcs/handlers/on_stage_complete.yml
trigger:
  event: task.stage_completed
  filter:
    stage: implementing

actions:
  - VCS.commit:
      message: "feat: {{task.context.description}}"
      files: ["src/**", "tests/**"]

# file-kanban/handlers/on_stage_complete.yml  
trigger:
  event: task.stage_completed

actions:
  - KANBAN.update_task:
      task_id: "{{task.id}}"
      state: "{{task.state}}"
```

**Результат:** Пакеты декаплированы, легко добавлять/удалять.

#### Расширяемость

**Создать свой пакет:**

1. Определить концепт (если нужно)
2. Создать implementation
3. Зарегистрировать в project.yml
4. Использовать

**Пример: Jira integration**

```yaml
# packages/implements/jira-integration/package.yml
name: jira-integration
type: implementation
depends_on:
  - core-concept
  - basic-events

provides:
  handlers:
    - on_task_created: Create Jira issue
    - on_task_completed: Close Jira issue
```

#### Как это работает в CM

**project.yml:** Список активных пакетов

**Dependency resolution:** Автоматическая загрузка зависимостей

**Event bus:** Связь между пакетами

**Результат:** Гибкость, расширяемость, переиспользование.

**Подробнее:** Модульность поддерживается мета-рантайм архитектурой ClearMethod. Каждый пакет может использовать разные режимы исполнения (AI, скрипты, оркестрация, интеграции) в зависимости от своих задач. См. [ClearMethod как мета-рантайм](clearmethod-как-мета-рантайм.md).

---

### 7. Безопасность по Умолчанию (Fail-Safe)

> **"Проверки перед действиями. Откат при ошибках. Compliance встроен"**

#### Суть принципа

AI может ошибаться. Код может ломаться. Данные могут течь. **Fail-Safe** означает: система построена так, чтобы минимизировать последствия проблем.

#### Три уровня защиты

**1. Quality Gates (проверки качества)**

```yaml
# qa-gates/config
checks:
  linter:
    command: npm run lint
    blocking: true
    
  tests:
    command: npm test
    blocking: true
    
  coverage:
    command: npm run coverage
    threshold: 80
    blocking: true
```

Проваленный gate блокирует переход:

```
implementing → verifying

QA_GATE.check():
  ✓ Linter: passed
  ✗ Tests: 3 failed
  
Transition: BLOCKED

AI: "Тесты провалились. Не могу перейти к verifying.
     Ошибки:
     1. AuthService.test.ts: Expected 200, got 401
     2. UserService.test.ts: Token validation failed
     3. ...
     
     Исправить автоматически?"
```

**2. Audit Trail (compliance)**

```markdown
# .cm/tasks/TASK-001/log.md

Полная история действий:
- Кто создал задачу
- Какие решения приняты
- Какие изменения сделаны
- Какие проверки пройдены
- Когда и кем завершена
```

Для regulated industries (finance, healthcare):
- GDPR compliance: audit trail всех действий
- HIPAA compliance: кто и когда имел доступ
- SOC 2: документированные процессы

**3. Safe Operations (уровни риска)**

```
Level 0 (read): Без ограничений
  - Чтение файлов
  - Поиск по коду
  - Анализ

Level 1 (safe): Автоматически
  - Форматирование
  - Добавление тестов
  - Обновление docs

Level 2 (moderate): С подтверждением
  - Рефакторинг
  - Изменение логики
  - Добавление зависимостей

Level 3 (risky): Explicit approval + review
  - Изменение API
  - Миграции БД
  - Security config

Level 4 (dangerous): Запрещено AI
  - Удаление данных
  - Force push
  - Production changes
```

#### Защита данных

**PII Detection (planned):**

```
⚠️  Detected PII in code:
    - Email addresses: 3
    - Phone numbers: 1
    
Options:
  1. Sanitize before sending to AI
  2. Block sending
  3. Add to whitelist (test data)
```

**Secret Detection:**

```
⛔ CRITICAL: API key detected
   File: config/database.js
   Line: 5
   
❌ Refusing to send to AI.

Recommendation: Use environment variables
```

#### Rollback и checkpoint

**После каждого stage:**

```yaml
Stage completed:
  ✓ Code committed
  ✓ Tests passed
  ✓ State updated
  ✓ Checkpoint saved

If next stage fails:
  → Can rollback to this checkpoint
```

**VCS integration:**

```
Checkpoint = Git commit

implementing stage:
  → Commit 1: Add AuthService
  → Commit 2: Add tests
  → Commit 3: Integrate with API
  
If verifying fails:
  → Can revert to any commit
```

#### Как это работает в CM

**qa-gates:** Автоматические проверки качества

**Audit logging:** Полная история в log.md

**Safety levels:** Контроль опасных операций

**VCS integration:** Автоматические коммиты как checkpoints

**PII protection (planned):** Санитизация чувствительных данных

**Результат:** Безопасная работа, compliance, возможность отката.

---

## Взаимосвязь принципов

Принципы не существуют изолированно — они поддерживают и усиливают друг друга:

```
От Диалога к Протоколу
    ↓ (делает возможным)
Прозрачность Состояния
    ↓ (обеспечивает)
Проверяй, Не Верь
    ↓ (требует)
Безопасность по Умолчанию

Контекст — Это Валюта
    ↓ (поддерживает)
Модульная Композиция
    ↓ (дает)
Человек Управляет
```

### Примеры синергии

**1. Протокол + Прозрачность = Воспроизводимость**

Декларативный протокол (CML) делает состояние явным → любой может воспроизвести процесс.

**2. Проверки + Безопасность = Качество**

Quality gates проверяют результат → безопасность блокирует проблемы → качество гарантировано.

**3. Модульность + Контекст = Эффективность**

Загружаешь только нужные пакеты → контекст минимален → работа эффективна.

### Разрешение конфликтов

**Конфликт: Скорость vs Качество**

Принципы говорят:
- **Проверяй, Не Верь:** Качество обязательно
- **Модульная Композиция:** Можно упростить scope

Решение: Упрости фичу до MVP, но с тестами и проверками.

**Конфликт: Автоматизация vs Контроль**

Принципы говорят:
- **Безопасность по Умолчанию:** Критическое нельзя автоматизировать
- **Человек Управляет:** Финальное решение за человеком

Решение: Автоматизируй безопасное, контролируй критическое.

---

## Принципы на практике

### Сценарий 1: Начало работы над фичей

**Без принципов:**
```
→ Открываю ChatGPT
→ "Сделай авторизацию"
→ Копирую результат
→ Не работает
→ Начинаю дебажить
```

**С принципами:**
```
→ cm start "Add two-factor authentication"

[От Диалога к Протоколу]
→ Создается Task с workflow

[Прозрачность Состояния]
→ State: creating, нужны specs

[Контекст — Это Валюта]
→ Загружается только нужный контекст

[Проверяй, Не Верь]
→ AI проверяет существующую авторизацию

[Человек Управляет]
→ Утверждаю specs перед переходом

[Безопасность по Умолчанию]
→ Quality gates проверят результат

Результат: Структурированный процесс, качественный код
```

### Сценарий 2: Рефакторинг legacy кода

**Без принципов:**
```
→ "Отрефактори этот файл"
→ AI переписывает всё
→ Ломаются зависимости
→ Теряется логика
```

**С принципами:**
```
[Проверяй, Не Верь]
→ Сначала анализ без изменений
→ AI проверяет зависимости

[Человек Управляет]
→ Review плана рефакторинга
→ Approval перед началом

[Модульная Композиция]
→ Incremental подход по модулям

[Безопасность по Умолчанию]
→ Тесты до и после
→ Возможность rollback

Результат: Безопасный рефакторинг, ничего не сломано
```

### Сценарий 3: Командная разработка

**Без принципов:**
```
Alice: Работает в ChatGPT
Bob: Пытается понять что она сделала
→ История потеряна
→ Повторная работа
```

**С принципов:**
```
[Прозрачность Состояния]
Alice: Работает через Task
→ Вся история в log.md
→ Артефакты сохранены

[От Диалога к Протоколу]
Bob: Подхватывает Task
→ Понятно где остановилась Alice
→ Продолжает с точного места

[Модульная Композиция]
file-kanban показывает статус всех задач
→ Команда видит прогресс

Результат: Эффективная командная работа
```

---

## Заключение

### Принципы как фундамент

Эти семь принципов — не просто теория. Они определяют:
- **Как структурирована работа** (От Диалога к Протоколу)
- **Как гарантируется качество** (Проверяй, Не Верь + Безопасность)
- **Как управляется сложность** (Контекст + Модульность)
- **Как сохраняется контроль** (Человек Управляет + Прозрачность)

Без этих принципов ClearMethod — просто набор инструментов. С ними — это **методология**, которая делает работу с AI предсказуемой, качественной и профессиональной.

### От хаоса к порядку

```
Хаос:
  Промпты → Надежда → Непредсказуемость → Технический долг

Порядок (ClearMethod):
  Протокол → Процесс → Проверки → Качество
```

### Эволюция принципов

Принципы не высечены в камне. Они эволюционируют:
- С развитием AI технологий
- С накоплением опыта сообщества
- С изменением best practices

Но **ядро остается:** структура, проверка, контроль, прозрачность.

### Следующие шаги

После понимания принципов:
1. Изучи **концепты** (TASK, WORKFLOW, EVENT, etc.)
2. Познакомься с **пакетами** (что доступно)
3. Попробуй на **практике** (quick start)

**Помни:** Принципы ClearMethod — это про **партнерство с AI**, **структуру в процессах** и **качество в результате**.

Всё остальное — детали реализации.

---

**Готов начать?** Переходи к документу [концепты](../03-архитектура/обзор.md) или [быстрый старт](../07-руководства/быстрый-старт.md).

