# Антипаттерны: чего избегать

## Введение

Даже с лучшими инструментами и намерениями легко скатиться в неэффективные паттерны работы. ClearMethod предлагает структуру, но структура сама по себе не гарантирует успеха — важно **как** ей пользоваться.

Этот документ описывает типичные антипаттерны (anti-patterns) — распространенные ошибки, которые снижают эффективность работы с AI и приводят к проблемам.

**Зачем знать антипаттерны?**
- Чтобы не повторять чужих ошибок
- Чтобы быстрее находить проблемы в своем процессе
- Чтобы понимать "почему так не надо"

---

## Антипаттерн 1: "AI знает лучше"

### Описание

Слепое доверие решениям AI без проверки, обсуждения или понимания обоснования.

### Проявления

**Пример 1:**
```
AI: "Предлагаю использовать MongoDB вместо PostgreSQL"
You: "Ок, делай"
[Без вопросов, без проверки контекста]
```

**Пример 2:**
```
AI: "Рефакторинг завершен. Изменено 47 файлов."
You: "Отлично, коммитим"
[Не проверил ни одного файла]
```

**Пример 3:**
```
AI: "Добавил кеширование на уровне БД"
You: "Супер!"
[Не понял как работает, не проверил последствия]
[Через неделю: memory leaks в production]
```

### Почему это опасно

❌ **AI не знает бизнес-контекст**
```
AI предлагает: Microservices
Реальность: Команда 2 человека, нет DevOps
Результат: Operational nightmare
```

❌ **AI не знает ограничения инфраструктуры**
```
AI предлагает: Kafka для event streaming
Реальность: Shared hosting, нет Docker
Результат: Невозможно развернуть
```

❌ **AI может галлюцинировать**
```
AI: "Используем библиотеку super-auth (лучшая в индустрии)"
Реальность: Библиотеки не существует
Результат: Код не компилируется
```

❌ **AI не несет ответственность**
```
Если что-то сломается в production, отвечаешь ты, не AI.
```

### Последствия

- Неподходящие архитектурные решения
- Технический долг
- Production инциденты
- Потеря доверия команды к процессу

### Как правильно

✅ **Всегда спрашивай "почему"**
```
AI: "Предлагаю использовать MongoDB вместо PostgreSQL"

You: "Почему MongoDB? Обоснуй выбор.
      У нас PostgreSQL везде, команда знает SQL,
      есть DBA. Смена БД — это огромные изменения."

AI: "Вы правы. Учитывая:
      - Существующую инфраструктуру (PostgreSQL)
      - Знания команды (SQL)
      - Наличие DBA
      - Отсутствие требований к document-oriented хранению
      
      PostgreSQL — правильный выбор.
      Миграция на MongoDB не оправдана для данной задачи."
```

✅ **Проверяй критические изменения**
```
AI: "Рефакторинг завершен. Изменено 47 файлов."

You: [Проверяет ключевые файлы]
     [Запускает тесты]
     [Проверяет что ничего не сломалось]
     "Approve"
```

✅ **Требуй объяснений**
```
AI: "Добавил кеширование"

You: "Объясни:
      - Где именно кеширование?
      - Что кешируется?
      - Какой TTL?
      - Как инвалидация?
      - Какие риски?"
```

### Правило

> **Доверяй, но проверяй.**  
> AI — партнер, но финальная ответственность за тобой.

---

## Антипаттерн 2: "AI — это калькулятор"

### Описание

Использование AI только для генерации фрагментов кода, без использования полного потенциала: анализа, проектирования, документирования.

### Проявления

**Пример 1:**
```
You: "Напиши функцию для валидации email"
AI:  [Generates function]
You: [Copy-paste в код, done]
```

**Пример 2:**
```
You: "Добавь this.users.push(user)"
AI:  [Adds line]
You: "Теперь добавь import"
AI:  [Adds import]
[Микрозадачи вместо полного процесса]
```

**Пример 3:**
```
You: "Вот код. Сделай его быстрее."
AI:  [Optimizes code]
You: [Copy-paste]
[Нет понимания что изменилось и почему]
```

### Почему это неэффективно

❌ **Недоиспользование возможностей**
```
AI может:
  ✓ Проанализировать где нужна валидация
  ✓ Учесть существующие паттерны в проекте
  ✓ Добавить тесты
  ✓ Обновить документацию
  ✓ Проверить consistency

Вы используете:
  ✗ Только генерацию кода
```

❌ **Точечные решения, не системные**
```
Результат "калькуляторного" подхода:
  - Дублирование логики
  - Inconsistency в стилях
  - Отсутствие тестов
  - Нет документации
  - Непонятно "почему так"
```

❌ **Потеря контекста**
```
Каждое обращение — без контекста предыдущих.
Нет целостного видения задачи.
```

### Последствия

- Низкое качество кода
- Технический долг
- Много времени на интеграцию
- Inconsistency в кодовой базе

### Как правильно

✅ **Используй полный workflow**
```
Вместо:
  You: "Напиши функцию для валидации email"
  AI:  [Function]

Делай:
  You: /cm start feature "email validation"
  
  AI: [Analyzing]
      - Нашел 3 места где требуется валидация
      - Есть существующая валидация для phone (можно переиспользовать паттерн)
      - Нужна валидация на клиенте + сервере
      
      [Designing]
      - Utility function в /utils/validation
      - Интеграция с существующей системой
      - Тесты с edge cases
      
      [Implementing]
      - validateEmail() + tests
      - Интеграция в формы
      - Server-side validation
      - Документация
```

✅ **Дай AI увидеть полную картину**
```
You: /cm start refactoring "optimize user service"

AI: [Анализирует весь UserService]
    [Выявляет паттерны]
    [Предлагает системное решение]
    [Реализует с тестами]
    [Обновляет все зависимости]
```

✅ **Используй AI для понимания, не только для кода**
```
You: "Почему этот код медленный?"

AI: [Анализирует]
    "Проблема: N+1 queries.
     На каждого пользователя — отдельный запрос к БД.
     При 1000 пользователях — 1000 запросов.
     
     Решение: использовать .include() для eager loading.
     Вместо 1000 запросов — 1 запрос."

[Ты понял проблему, научился]
```

### Правило

> **AI — не калькулятор, а партнер в полном процессе.**  
> Используй его для анализа, проектирования, реализации, документирования.

---

## Антипаттерн 3: "Я все контролирую"

### Описание

Микроменеджмент каждого действия AI, разбивая задачу на мельчайшие шаги и диктуя каждый из них.

### Проявления

**Пример 1:**
```
You: "Создай переменную users"
AI:  [Creates variable]
You: "Теперь добавь функцию getUsers"
AI:  [Adds function]
You: "Теперь добавь параметр limit"
AI:  [Adds parameter]
You: "Теперь добавь default value 10"
AI:  [Adds default]
You: "Теперь добавь JSDoc"
AI:  [Adds JSDoc]
[20 микроинструкций вместо одной задачи]
```

**Пример 2:**
```
You: "Измени эту строку на..."
AI:  [Changes]
You: "Теперь ту строку на..."
AI:  [Changes]
You: "Теперь добавь import..."
AI:  [Adds]
[Ты выполняешь работу, AI — просто typing tool]
```

### Почему это неэффективно

❌ **Медленно**
```
20 микроинструкций: ~30 минут
1 полная задача AI: ~5 минут
```

❌ **AI не видит полную картину**
```
AI получает:
  "Добавь параметр limit"

AI не знает:
  - Зачем limit?
  - Какой range допустим?
  - Нужна ли валидация?
  - Как влияет на другой код?
  
Результат: Неоптимальное решение
```

❌ **Ты делаешь работу AI**
```
Если ты знаешь каждый шаг заранее:
  - Зачем тебе AI?
  - Быстрее написать самому
  
Смысл AI:
  - Ты даешь цель
  - AI разбивает на шаги
  - AI реализует
```

❌ **Усталость и фрустрация**
```
Микроменеджмент утомителен для обеих сторон.
AI может больше, ты тратишь время зря.
```

### Последствия

- Потеря времени
- Неоптимальные решения (AI не видит контекст)
- Упущенные возможности
- Фрустрация

### Как правильно

✅ **Дай AI автономию в рамках задачи**
```
Вместо:
  You: "Создай users"
  You: "Добавь getUsers"
  You: "Добавь limit"
  ...

Делай:
  You: "Реализуй функционал управления пользователями согласно design.md"
  
  AI: [Анализирует design.md]
      [Реализует полностью:]
        - User model
        - CRUD operations
        - Validation
        - Tests
        - Documentation
      
  You: [Проверяешь результат]
       [Даешь фидбек если нужно]
```

✅ **Указывай "что", не "как"**
```
❌ Плохо:
  "Добавь try-catch вокруг этого вызова, 
   а в catch выведи console.error,
   а потом throw new Error..."

✅ Хорошо:
  "Добавь error handling для этой функции"
  
  [AI сам решит как: try-catch, правильный logging, appropriate error types]
```

✅ **Доверяй + Проверяй результат**
```
You: "Реализуй authentication согласно плану"

AI: [Реализует за 10 минут:]
    - Auth routes
    - JWT generation/verification  
    - Password hashing
    - Tests
    - Documentation

You: [Проверяешь:]
     ✓ Запускает тесты: passed
     ✓ Проверяет ключевой код: looks good
     ✓ Проверяет security: bcrypt rounds OK, JWT secret from env
     
     "Approve"
     
[Результат: 10 минут вместо часа микроменеджмента]
```

### Правило

> **Управляй на уровне задач, не на уровне строк кода.**  
> Дай AI автономию в рамках задачи. Проверяй результат, не процесс.

---

## Антипаттерн 4: "Пропускаем workflow"

### Описание

Пропуск этапов workflow "чтобы быстрее": сразу в implementing, без analyzing и designing.

### Проявления

**Пример 1:**
```
You: "Давай сразу в implementing, без analyzing"
     [Чтобы "сэкономить время"]

AI: [Реализует]
    [Потом оказывается: не учел edge cases]
    [Потом оказывается: неоптимальный подход]
    [Переделка занимает в 3 раза больше времени]
```

**Пример 2:**
```
You: "Designing не нужен, задача простая"

AI: [Сразу кодит]
    [Выбирает первый пришедший в голову подход]
    [Он работает... но через месяц создает проблемы]
```

**Пример 3:**
```
You: "Нет времени на planning, давай сразу делать"

AI: [Начинает implementing]
    [Забывает про файл X]
    [Забывает про dependency Y]
    [Забывает обновить тесты]
    [Результат: incomplete implementation]
```

### Почему это неэффективно

❌ **Analyzing предотвращает неправильное понимание**
```
Без analyzing:
  Задача: "Add caching"
  AI делает: кеширует все подряд
  Проблема: memory overflow
  
С analyzing:
  AI: "Проанализировал. Узкое место — user permissions.
       Предлагаю кешировать только их (5MB), не все данные (500MB)."
  Результат: Правильное решение
```

❌ **Designing предотвращает плохую архитектуру**
```
Без designing:
  AI: [Реализует первое что пришло в голову]
  Результат: Работает, но не масштабируется / не поддерживается
  
С designing:
  AI: [Рассматривает варианты]
      [Выбирает оптимальный]
      [Обосновывает]
  Результат: Правильная архитектура
```

❌ **Planning предотвращает забытые части**
```
Без planning:
  AI: [Кодит]
  Забывает: тесты, документацию, edge cases
  
С planning:
  AI: [Создает checklist]
      [Ничего не забыто]
```

❌ **"Быстрее" оборачивается "медленнее"**
```
Пропустил workflow: 15 минут на coding
Потом: 2 часа на debugging + 1 час на рефакторинг

Следовал workflow: 30 минут всего
Результат: правильно с первого раза
```

### Последствия

- Неполное понимание задачи
- Плохие архитектурные решения
- Забытые edge cases
- Больше времени на переделки
- Технический долг

### Как правильно

✅ **Следуй workflow даже для простых задач**
```
Простая задача: "Add button"

Кажется: зачем analyzing/designing для кнопки?

Но analyzing показывает:
  - Где кнопка? (3 места, не 1)
  - Какой стиль? (должна соответствовать design system)
  - Какой action? (нужна интеграция с API)
  - Accessibility? (нужны aria-labels)

Результат: 5 минут на analyzing
           Правильная реализация с первого раза
           
vs пропустил:
           5 минут на "быструю" реализацию
           30 минут на фиксы после review
```

✅ **Адаптируй глубину workflow под сложность**
```
Простая задача:
  Analyzing: 2 минуты
  Designing: 3 минуты  
  Planning: 2 минуты
  Implementing: 5 минут
  Total: 12 минут

Сложная задача:
  Analyzing: 30 минут
  Designing: 1 час
  Planning: 30 минут
  Implementing: 2 часа
  Total: 4 часа

Но этапы есть всегда!
```

✅ **Используй lightweight stages для мелких задач**
```
/cm start bugfix "Fix typo"

Analyzing: [Quick check]
  - Typo в README.md
  - Straightforward fix
  
Designing: [Minimal]
  - Just fix the typo
  
Implementing:
  - Fixed
  
Verifying:
  - Spell check passed
  
Done.

[Total: 2 минуты, но структурированно]
```

### Правило

> **Workflow существует не просто так.**  
> Даже для простых задач. Адаптируй глубину, но не пропускай этапы.

---

## Антипаттерн 5: "Нет проверки reality"

### Описание

Отсутствие верификации фактов, которые сообщает AI. Принятие hallucinations за реальность.

### Проявления

**Пример 1:**
```
AI: "Используем функцию getUserById() из utils"
You: "Ок"

[Функции getUserById() не существует]
[AI hallucination]
[Code не компилируется]
```

**Пример 2:**
```
AI: "Обновил library X до версии 3.0"
You: "Отлично"

[Версии 3.0 не существует, latest — 2.5]
[npm install fails]
```

**Пример 3:**
```
AI: "API endpoint GET /api/v2/users поддерживает параметр filter"
You: [Использует в коде]

[Параметр filter не поддерживается]
[Production error: 400 Bad Request]
```

### Почему это опасно

❌ **AI может галлюцинировать**
```
AI иногда:
  - "Придумывает" функции/библиотеки
  - "Помнит" несуществующие API
  - "Знает" устаревшую информацию
  - Смешивает разные проекты
```

❌ **Hallucinations попадают в production**
```
Without reality checks:
  AI: "Используем функцию X"
  → Code committed
  → CI passes (nет такой проверки)
  → Deployed
  → Runtime error в production
```

❌ **Debugging занимает часы**
```
Ты debuggишь почему не работает...
...а функции просто не существует.

Reality check занял бы 5 секунд.
```

### Последствия

- Production ошибки
- Потеря времени на debugging
- Недоверие к процессу
- Broken builds

### Как правильно

✅ **Включи Reality Checks в workflow**
```yaml
# .cm/project.yml

quality_gates:
  - gate: reality_check
    enabled: true
    checks:
      - function_exists
      - file_exists
      - api_endpoint_exists
      - library_version_exists
```

✅ **AI должен сам проверять факты**
```
AI: "Используем функцию getUserById()"

[Reality check: поиск в кодовой базе]

AI: "ВНИМАНИЕ: getUserById() не найден в кодовой базе.
     
     Найдено похожее:
     - fetchUser(userId) в /services/user.js
     - getUser(id) в /utils/userHelpers.js
     
     Возможно имелась в виду одна из них?
     Или нужно создать новую функцию getUserById()?"
```

✅ **Верифицируй критические утверждения**
```
AI: "Обновил библиотеку react до версии 19.0"

Reality check:
  $ npm info react version
  → 18.2.0 (latest)
  
AI: "КОРРЕКЦИЯ: Ошибся. Latest версия react — 18.2.0.
     Обновляю до 18.2.0."
```

✅ **Проверяй перед deploy**
```
Before production:
  ✓ Запусти тесты (проверят function calls)
  ✓ Проверь imports (проверят что файлы существуют)
  ✓ Type check (проверит API соответствие)
  ✓ Staging deploy (проверит runtime behavior)
```

### Правило

> **Доверяй, но проверяй. Reality checks обязательны.**  
> AI может ошибаться. Верификация должна быть встроена в процесс.

---

## Антипаттерн 6: "Игнорирование Quality Gates"

### Описание

Пропуск или игнорирование quality gates "чтобы быстрее задеплоить".

### Проявления

**Пример 1:**
```
Quality Gate: Tests failed (3/50 failed)
You: "Ладно, потом пофиксим. Deploy."

[Deploy в production с broken tests]
[Production incident]
```

**Пример 2:**
```
Quality Gate: Coverage 45% (требуется 80%)
You: "Ок, тесты напишем потом"

["Потом" никогда не наступает]
[Через месяц: untested кодовая база]
```

**Пример 3:**
```
Quality Gate: Linter errors (15 errors)
You: "Это мелочи, игнорируем"

[Linter errors маскируют реальный баг]
[Bug обнаружен в production]
```

### Почему это опасно

❌ **Ломается сразу или потом**
```
Пропустил тесты → bug в production
Пропустил coverage → untestable код → рефакторинг страшно делать
Пропустил linter → code quality деградирует
```

❌ **"Потом" никогда не наступает**
```
"Тесты напишем потом" = тестов не будет
"Рефакторинг сделаем потом" = технический долг
"Документацию добавим потом" = нет документации
```

❌ **Скатывание в хаос**
```
Один раз пропустил → "в прошлый раз же ок было"
Второй раз → "это же быстрый hotfix"
Третий раз → качество больше не важно
Результат: legacy codebase через полгода
```

### Последствия

- Production инциденты
- Технический долг
- Деградация качества
- Невозможность поддерживать

### Как правильно

✅ **Gates блокируют по умолчанию**
```yaml
quality_gates:
  - gate: tests
    blocking: true  # Нельзя продолжить если failed
    
  - gate: coverage
    blocking: true
    min_coverage: 80%
    
  - gate: linter
    blocking: true
```

✅ **Исправляй, не пропускай**
```
Quality Gate: Tests failed

Неправильно:
  "Deploy anyway"

Правильно:
  "Почему failed? Пофиксим."
  [Фиксит тесты]
  [Или тесты неправильные — обновит тесты]
  [Gate passed → deploy]
```

✅ **Настрой gates под проект**
```yaml
# Строгие gates для critical services
quality_gates:
  - gate: tests
    min_coverage: 90%
    blocking: true
    
  - gate: security_scan
    blocking: true
    
  - gate: performance_test
    blocking: true

# Более мягкие gates для internal tools  
quality_gates:
  - gate: tests
    min_coverage: 70%
    blocking: true
    
  - gate: linter
    blocking: false  # Warning only
```

### Правило

> **Quality gates существуют чтобы блокировать проблемы.**  
> Не пропускай их. Если gate failed — исправь причину.

---

## Антипаттерн 7: "Нет документирования решений"

### Описание

Отсутствие документирования "почему" было принято решение. Код есть, а контекст потерян.

### Проявления

**Пример 1:**
```
Code:
  if (user.age < 21) {
    return "restricted";
  }

Вопрос через 3 месяца: Почему 21? Legal requirement? Business rule?
Ответ: Никто не помнит.
```

**Пример 2:**
```
Git history:
  "Added caching"
  "Fixed caching"  
  "Removed caching"
  
Почему удалили? Что было не так?
Ответ: Lost to history.
```

**Пример 3:**
```
Новый разработчик:
  "Почему мы используем PostgreSQL, а не MySQL?"
  
Команда:
  "Хороший вопрос... не помню."
```

### Почему это проблема

❌ **Контекст решений теряется**
```
Через месяц:
  - Почему выбрали этот подход?
  - Какие альтернативы рассматривали?
  - Какие были trade-offs?
  
Ответ: Не знаем. Надо разбираться заново.
```

❌ **Повторяются старые ошибки**
```
Решение X было отклонено год назад (веские причины).
Новый dev предлагает решение X.
Причины забыты → делают X снова.
Результат: Повтор старых проблем.
```

❌ **Сложно onboarding**
```
Новый разработчик:
  - Как это работает? Почему?
  - Какие были design decisions?
  
Единственный способ: читать код и гадать.
```

### Последствия

- Потеря знаний
- Повторение ошибок
- Сложный onboarding
- Невозможность оспорить решения

### Как правильно

✅ **ClearMethod документирует автоматически**
```yaml
# .cm/tasks/TASK-042/

analysis.md:
  ## Problem Analysis
  Current auth: JWT без refresh tokens.
  Issue: Users логаутятся каждые 15 минут (token expiry).
  Business impact: Плохой UX, complaints.

design.md:
  ## Solution: Refresh Tokens
  
  Decision: Add refresh tokens (7 days lifetime)
  
  Alternatives considered:
    1. Longer JWT lifetime (24h)
       Rejected: Security risk
       
    2. Sessions in Redis
       Rejected: Добавляет complexity, stateful
       
    3. Refresh tokens
       Selected: Best balance security/UX
  
  Trade-offs:
    + Better UX (stay logged in)
    + Secure (short-lived access token)
    - Added complexity (2 tokens vs 1)
    - Storage needed (refresh tokens)

log.md:
  ## 2024-01-15: Design Approved
  Chose refresh tokens approach.
  Reasoning: Security + UX balance.
  Alternatives rejected: longer JWT (insecure), sessions (complex).
```

✅ **Документируй "почему", не только "что"**
```
❌ Плохо:
  // Added caching

✅ Хорошо:
  // Added Redis caching for user permissions (15min TTL)
  // Reason: user permissions checked on every API call (20+ calls/page)
  // Impact: Reduced DB load 80%, improved response time 200ms → 20ms
  // Trade-off: Permissions changes delayed up to 15min (acceptable per PM)
```

✅ **Сохраняй ADR (Architecture Decision Records)**
```markdown
# ADR-001: Use PostgreSQL over MongoDB

## Context
Need to choose database for the project.

## Decision
Use PostgreSQL.

## Rationale
- Team knows SQL
- Relational data model fits our domain
- ACID guarantees needed
- Existing infrastructure (DBA, backups)

## Alternatives Considered
- MongoDB: Rejected (no need for document model, team unfamiliar)
- MySQL: Rejected (PostgreSQL has better JSON support for future)

## Consequences
+ Leverages team expertise
+ Proven technology
- Less flexible schema (mitigated by migrations)

## Date
2024-01-15
```

### Правило

> **Документируй решения, не только код.**  
> Через месяц/год контекст забудется. Design docs сохраняют "почему".

---

## Как избегать антипаттернов

### 1. Осознание
Первый шаг — знать что антипаттерны существуют.

**Действие:**
- Прочитай этот документ
- Узнай свои слабые места
- Будь внимателен к признакам

---

### 2. Code Review
Проверяй свои и чужие решения.

**Контрольные вопросы:**
- [ ] Проверил ли я утверждения AI? (Reality check)
- [ ] Понимаю ли я "почему" это решение? (Не "AI знает лучше")
- [ ] Следовал ли workflow? (Не пропустил этапы)
- [ ] Прошли ли quality gates? (Не игнорировал)
- [ ] Документированы ли ключевые решения? (Не потеряется контекст)

---

### 3. Ретроспективы
Анализируй что пошло не так.

**Пример:**
```
Проблема: Bug в production (забыли edge case)

Root cause: Пропустили analyzing stage

Lesson: Даже для "простых" задач — следовать workflow

Action: Добавить pre-commit hook: проверка что analyzing был выполнен
```

---

### 4. Настройка процесса
Адаптируй ClearMethod под себя, добавляй защиты от антипаттернов.

**Примеры:**
```yaml
# Защита от "пропускаем workflow"
workflow:
  enforce_all_stages: true  # Нельзя пропустить stage

# Защита от "нет reality checks"  
quality_gates:
  - gate: reality_check
    blocking: true

# Защита от "игнорирование gates"
quality_gates:
  - gate: tests
    blocking: true
    override: require_admin  # Только admin может override
```

---

### 5. Обучение команды
Если работаешь в команде — делись знаниями.

**Действия:**
- Проводи внутренние митапы
- Разбирай антипаттерны на примерах
- Добавь в onboarding материалы

---

## Заключение

### Антипаттерны vs Паттерны

```
┌─────────────────────────────────────────────────────┐
│ АНТИПАТТЕРНЫ (чего избегать)                        │
└─────────────────────────────────────────────────────┘

❌ "AI знает лучше" → слепое доверие
❌ "AI = калькулятор" → недоиспользование
❌ "Я все контролирую" → микроменеджмент
❌ "Пропускаем workflow" → хаос
❌ "Нет reality checks" → hallucinations в коде
❌ "Игнорируем gates" → деградация качества
❌ "Нет документации" → потеря контекста

────────────────────────────────────────────────────────

┌─────────────────────────────────────────────────────┐
│ ПАТТЕРНЫ (как правильно)                            │
└─────────────────────────────────────────────────────┘

✅ Доверяй, но проверяй
✅ Используй полный потенциал AI
✅ Управляй задачами, не строками
✅ Следуй workflow
✅ Проверяй реальность (reality checks)
✅ Уважай quality gates
✅ Документируй решения
```

---

### Ключевая мысль

> **Антипаттерны — это опыт.**  
> Свой или чужой. Лучше учиться на чужих ошибках.

ClearMethod предоставляет структуру, но структура не защищает от всех ошибок. Осознанное использование, понимание "почему так", и избегание антипаттернов — путь к эффективной работе с AI.

---

## Следующие шаги

📖 **Изучи философию:**
- [Принципы](принципы.md) — Фундаментальные принципы ClearMethod
- [Партнерство с AI](партнерство-с-ai.md) — Как строить эффективное взаимодействие
- [Манифест](манифест.md) — Ценности и видение

🏗 **Понять архитектуру:**
- [Обзор архитектуры](../03-архитектура/обзор.md)
- [Ядро](../03-архитектура/ядро.md)

🚀 **Начать практику:**
- [Быстрый старт](../07-руководства/быстрый-старт.md)
- [Первая задача](../07-руководства/первая-задача.md)

