# Команда: /cm-start

> **Для пользователей**: Создание задачи и запуск workflow

---

## Назначение

Создает новую задачу и запускает для нее workflow.

---

## Использование

```
/cm-start [workflow-id] [task-id] [title]
```

**Параметры** (все опциональны - агент запросит недостающие):
- `workflow-id` - ID workflow (например, `sbd.feature`)
- `task-id` - уникальный ID задачи (например, `TASK-123`)
- `title` - название задачи

---

## Примеры

### Все параметры указаны
```
/cm-start sbd.feature TASK-123 "Add notification system"
```

### Часть параметров (агент запросит остальное)
```
/cm-start sbd.feature TASK-123
Agent: "Task title?"
User: "Add notification system"
```

### Без параметров (интерактивный режим)
```
/cm-start
Agent: "Which workflow? (e.g. sbd.feature)"
User: "sbd.feature"
Agent: "Task ID? (e.g. TASK-123)"
User: "TASK-123"
Agent: "Task title?"
User: "Add notification system"
```

---

## Что происходит

1. **Валидация**
   - Проверка что workflow существует
   - Проверка что задача с таким ID еще не создана
   - Валидация формата task-id

2. **Создание задачи**
   - Вызов `FILE_TASK.create(task-id, title, ...)`
   - Создается папка `.cm/tasks/TASK-123/`
   - Создаются файлы: `status.yml`, `specs.md`, `log.md`

3. **Запуск workflow**
   - Вызов `WORKFLOW.start(task-id)`
   - Задача переходит в начальное состояние workflow
   - Выполняется первое состояние (например, `creating`)

4. **Выполнение состояния**
   - Агент начинает работу в соответствии с состоянием
   - Для `creating`: диалог о требованиях, заполнение specs.md

---

## После выполнения

- Задача создана в `.cm/tasks/TASK-123/`
- Workflow запущен
- Агент работает в первом состоянии
- Можно использовать `/cm-next` для перехода далее

---

## Примеры выполнения

### Успех

```
User: /cm-start sbd.feature TASK-123 "Add notifications"

Agent:
✅ Task TASK-123 created
📁 Location: .cm/tasks/TASK-123/

Starting workflow: sbd.feature
→ Stage: creating

Let's define the requirements for this task.
What problem does this feature solve?
...
```

### Ошибка - задача уже существует

```
User: /cm-start sbd.feature TASK-123

Agent:
❌ Error: Task TASK-123 already exists

Location: .cm/tasks/TASK-123/
Status: designing

Use /cm-status to see current state.
```

### Ошибка - workflow не найден

```
User: /cm-start unknown.workflow TASK-123

Agent:
❌ Error: Workflow 'unknown.workflow' not found

Available workflows:
- sbd.feature (Feature Development)

Check workflow ID and try again.
```

---

## Валидация task-id

Формат: `PREFIX-NUMBER`

Примеры:
- ✅ `TASK-123`
- ✅ `FEAT-42`
- ✅ `BUG-007`
- ❌ `task123` (нет дефиса)
- ❌ `123` (нет префикса)

---

## Связанные команды

После создания задачи:
- `/cm-next` - перейти к следующему этапу
- `/cm-status` - посмотреть статус
- `/cm-goback` - вернуться назад (если нужно)

---

## Важные детали

### Диалог с пользователем

В первом состоянии (`creating`) агент будет задавать вопросы:
- В чем суть задачи?
- Кто пользователь?
- Критерии приемки?
- Ограничения?

**Отвечайте подробно** - качественные требования = половина успеха.

### Артефакты

После создания в папке задачи:
```
.cm/tasks/TASK-123/
  status.yml    - свойства задачи
  specs.md      - спецификация (заполняется в creating)
  log.md        - журнал действий
```

### Одна задача за раз

Рекомендуется работать с одной задачей в рамках одного чата.
Для новой задачи лучше начать новый чат с `/cm-prime`.

---

**Команда /cm-start - точка входа в workflow. После нее начинается реальная работа.**

