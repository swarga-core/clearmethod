# Hello World Extension

> Тестовое расширение ClearMethod для проверки механик и обучения

---

## Зачем?

Это расширение создано для:

✅ **Тестирования фреймворка** - проверка всех механик ClearMethod  
✅ **Демонстрации концептов** - альтернативная реализация TASK  
✅ **Обучения пользователей** - простые примеры для понимания  
✅ **Экспериментов** - безопасная песочница для новых идей  

⚠️ **НЕ используйте для реальной работы!** Используйте `file-task` + `sbd`.

---

## Что включено?

### 1. HELLO_TASK

Упрощенная реализация концепта TASK:

- **Один YAML файл** вместо папки
- **Формат**: `.cm/tasks/TASK-123.yml`
- **Содержит**: id, title, status, properties, log (всё в одном файле)

**Пример файла**:
```yaml
id: TEST-001
title: "Hello World Test"
workflow: "hello-world.simple"
status: "process"
created_at: "2025-11-24T10:00:00Z"
created_by: "Developer"
properties:
  test_mode: true
  counter: 42
log:
  - "[2025-11-24T10:00:00Z] Task created"
  - "[2025-11-24T10:05:00Z] Processing started"
```

### 2. Workflow: simple

Простой 3-этапный workflow для тестирования:

```
start → process → finish
```

**Что тестирует**:
- Variables (let:)
- Conditionals (if/then/else)
- Loops (for, while)
- Groups (group: + do:)
- Methods (TASK.*)
- Properties
- Logging
- String interpolation
- Validation
- User interaction

---

## Быстрый старт

### 1. Настройка project.yml

Добавьте в `.cm/project.yml`:

```yaml
extensions:
  - hello-world  # Добавить в список

concept_implementations:
  TASK: hello-world.HELLO_TASK  # Использовать HELLO_TASK
```

### 2. Обновите priming.yml

Добавьте primer расширения:

```yaml
primers:
  - path: extensions/hello-world/primer.md
    required: true
```

### 3. Запустите тест

```bash
# В IDE с ClearMethod (например, Cursor):
/cm-prime                               # Загрузить контекст
/cm-start hello-world.simple TEST-001  # Создать тест-задачу
/cm-next                                # → process
/cm-next                                # → finish
```

### 4. Проверьте результаты

```bash
cat .cm/tasks/TEST-001.yml
```

Вы увидите все изменения: установленные свойства, записи в логе, обновленный статус.

---

## Структура расширения

```
extensions/hello-world/
├── extension.yml          # Описание расширения
├── primer.md             # Краткий primer для агента
├── concept.yml           # HELLO_TASK (реализация TASK)
├── README.md             # Этот файл
├── methods/              # Методы HELLO_TASK
│   ├── create.yml
│   ├── get_state.yml
│   ├── set_state.yml
│   ├── get_property.yml
│   ├── set_property.yml
│   ├── log.yml
│   └── get_log.yml
└── workflows/
    └── simple/           # Простой тестовый workflow
        ├── workflow.yml
        └── states/
            ├── start.yml      # Инициализация
            ├── process.yml    # Обработка (тесты CML)
            └── finish.yml     # Завершение
```

---

## Сравнение с FILE_TASK

| Характеристика | FILE_TASK | HELLO_TASK |
|----------------|-----------|------------|
| **Хранение** | Папка с множеством файлов | Один YAML файл |
| **Путь** | `.cm/tasks/TASK-123/` | `.cm/tasks/TASK-123.yml` |
| **Артефакты** | Отдельные файлы (specs.md, design.md) | Всё в одном файле |
| **Лог** | Markdown файл (log.md) | Массив в YAML |
| **Сложность** | Реалистичная | Упрощенная |
| **Для production** | ✅ Да | ❌ Нет |
| **Для тестирования** | Можно | ✅ Идеально |

---

## Тестовые сценарии

### Сценарий 1: Базовый flow

Пройти все состояния:

```
/cm-start hello-world.simple TEST-001
/cm-next  # start → process
/cm-next  # process → finish
```

**Проверка**: Все состояния выполнены, свойства установлены, лог заполнен.

### Сценарий 2: Резолвинг концептов

Проверить, что вызовы `TASK.*` правильно резолвятся в `HELLO_TASK`:

1. Агент видит `TASK.set_property(...)`
2. Читает `project.yml` → `hello-world.HELLO_TASK`
3. Открывает `concept.yml` → находит `methods/set_property.yml`
4. Выполняет CML из метода

**Проверка**: Свойства устанавливаются в `.cm/tasks/TEST-001.yml`.

### Сценарий 3: CML конструкции

В состоянии `process` тестируются:
- if/then/else ✓
- for loops ✓
- while loops ✓
- group: ✓
- String interpolation ✓
- Validation ✓

**Проверка**: Все тесты проходят, записи в логе подтверждают выполнение.

### Сценарий 4: Переключение реализаций

Изменить `project.yml`:

```yaml
# Было:
concept_implementations:
  TASK: hello-world.HELLO_TASK

# Стало:
concept_implementations:
  TASK: file-task.FILE_TASK
```

Запустить тот же workflow:

```
/cm-start hello-world.simple TEST-002
```

**Проверка**: Workflow работает, но задача теперь хранится в папке `.cm/tasks/TEST-002/`.

---

## Ограничения

HELLO_TASK имеет ограничения:

❌ Один файл не подходит для больших задач  
❌ Лог может разрастись в огромный массив  
❌ Нет места для бинарных артефактов  
❌ Нет отдельных файлов для specs, design, test-results  
❌ Не подходит для Stage-Based Development (SBD)  

**Вывод**: Отлично для тестов, не подходит для production.

---

## Расширение возможностей

Вы можете добавить в `hello-world`:

### Новые workflows

Создайте `workflows/another/`:
- `workflow.yml`
- `states/...`

Добавьте в `extension.yml`:
```yaml
provides:
  workflows:
    - hello-world.simple
    - hello-world.another  # Новый workflow
```

### Новые методы

Добавьте метод в `concept.yml`:
```yaml
methods:
  my_custom_method: methods/my_custom_method.yml
```

Создайте `methods/my_custom_method.yml` с CML.

### Новые тестовые сценарии

Добавьте сценарии в `workflow.yml`:
```yaml
test_scenarios:
  - name: "My test"
    description: "Test something new"
```

---

## FAQ

### Можно ли использовать HELLO_TASK и FILE_TASK одновременно?

Нет. В `project.yml` может быть только одна реализация `TASK` активна:

```yaml
concept_implementations:
  TASK: hello-world.HELLO_TASK  # ИЛИ file-task.FILE_TASK
```

Но можно переключаться между ними!

### Что произойдет, если удалить маппинг из project.yml?

Агент не сможет резолвить `TASK.*` и выдаст ошибку:
```
"Концепт TASK не имеет активной реализации"
```

### Можно ли использовать hello-world.simple с FILE_TASK?

Да! Workflow не зависит от реализации:

```yaml
concept_implementations:
  TASK: file-task.FILE_TASK  # Используем FILE_TASK
```

```
/cm-start hello-world.simple TEST-003  # Workflow тот же
```

Задача будет храниться в папке `.cm/tasks/TEST-003/`.

### Зачем нужен hello-world, если есть FILE_TASK?

1. **Тестирование** - простая реализация находит баги быстрее
2. **Обучение** - легче понять на простом примере
3. **Демонстрация** - показывает, что можно иметь разные реализации
4. **Эксперименты** - безопасная песочница

---

## Вклад в развитие

Используйте `hello-world` для:

- Тестирования новых CML конструкций
- Проверки изменений в core
- Создания примеров для документации
- Воспроизведения багов

---

## См. также

- [Core Concepts](/docs/core/concepts.md) - Архитектура концептов
- [Configuration](/docs/core/configuration.md) - Настройка project.yml
- [CML Spec](/docs/core/cml/spec.md) - Спецификация CML
- [FILE_TASK](/extensions/file-task/) - Production-ready реализация
- [SBD](/extensions/sbd/) - Stage-Based Development workflows

---

**Happy testing!** 🧪

