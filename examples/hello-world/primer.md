# Hello World Extension Primer

> **Для ИИ-агента**: Тестовое пакет для проверки механик ClearMethod

---

## Что это?

**hello-world** - упрощенное тестовое пакет для:
- Тестирования механик ClearMethod
- Проверки резолвинга концептов
- Демонстрации альтернативной реализации TASK
- Обучения пользователей

**⚠️ НЕ ДЛЯ PRODUCTION!** Используйте `file-task` + `sbd` для реальной работы.

---

## HELLO_TASK

**Упрощенная реализация TASK**: один YAML файл вместо папки.

### Структура файла задачи

```yaml
# .cm/tasks/TASK-123.yml
id: TASK-123
title: "Test task"
workflow: "hello-world.simple"
status: "process"
created_at: "2025-11-24T10:00:00Z"
created_by: "John"
last_updated_at: "2025-11-24T10:05:00Z"
last_updater: "John"
properties:
  test_mode: true
  stage: "start"
  counter: 42
log:
  - "[2025-11-24T10:00:00Z] Task created"
  - "[2025-11-24T10:05:00Z] Start complete"
```

### Отличия от FILE_TASK

| FILE_TASK | HELLO_TASK |
|-----------|------------|
| Папка `.cm/tasks/TASK-123/` | Файл `.cm/tasks/TASK-123.yml` |
| Множество файлов (status.yml, log.md, specs.md, ...) | Один YAML файл |
| Лог в отдельном Markdown файле | Лог как массив в YAML |
| Артефакты в отдельных файлах | Всё в одном файле |

### Методы

Все стандартные методы TASK реализованы:
- `create(task_id, title, content, creator, workflow)`
- `get_state(task_id)` / `set_state(task_id, state)`
- `get_property(task_id, name)` / `set_property(task_id, name, value)`
- `log(task_id, entry)` / `get_log(task_id)`

**Использование**: Вызывай через абстракцию `TASK.*`, резолвинг через `project.yml`.

---

## Workflow: hello-world.simple

**Простой 3-этапный workflow** для тестирования.

### Граф состояний

```
start → process → finish
  ↑        ↓
  └────────┘ (опциональный возврат)
```

### Состояния

1. **start** - Инициализация и настройка
   - Тестирует базовые переменные
   - Устанавливает начальные свойства
   - Проверяет логирование

2. **process** - Основная обработка
   - Тестирует if/then/else
   - Тестирует for и while циклы
   - Тестирует group: + do:
   - Тестирует get/set свойств
   - Тестирует string interpolation

3. **finish** - Завершение
   - Собирает результаты тестов
   - Проверяет лог
   - Подводит итоги

### Что тестируется

✅ Variables (`let:`)
✅ Conditionals (`if/then/else`)
✅ Loops (`for`, `while`)
✅ Semantic grouping (`group:` + `do:`)
✅ Concept methods (`TASK.*`)
✅ Properties (get/set)
✅ Logging
✅ String interpolation (`{variable}`)
✅ Validation (`validate:`)
✅ User interaction (`ask:`)
✅ State isolation (через свойства)
✅ Preconditions / postconditions

---

## Использование

### Запуск тестового workflow

```
1. /cm-prime                    # Загрузить контекст
2. /cm-start hello-world.simple TEST-001  # Создать тест-задачу
3. /cm-next                     # → process
4. /cm-next                     # → finish
```

### Проверка результатов

После завершения workflow изучи файл:
```
.cm/tasks/TEST-001.yml
```

Там будут все установленные свойства и весь лог действий.

### Тестирование резолвинга

В `project.yml` должен быть маппинг:
```yaml
concept_implementations:
  TASK: hello-world.HELLO_TASK
```

Агент должен:
1. Увидеть вызов `TASK.set_property(...)`
2. Прочитать `project.yml` → найти `hello-world.HELLO_TASK`
3. Открыть `packages/hello-world/concept.yml`
4. Найти путь к методу: `methods/set_property.yml`
5. Выполнить CML из этого файла

---

## Переключение между реализациями

Можно тестировать переключение реализаций:

```yaml
# project.yml

# Вариант 1: Используем HELLO_TASK
concept_implementations:
  TASK: hello-world.HELLO_TASK

# Вариант 2: Используем FILE_TASK
concept_implementations:
  TASK: file-task.FILE_TASK
```

Workflow останется тем же, но хранилище задач изменится!

---

## Ограничения HELLO_TASK

❌ Не подходит для больших задач с множеством артефактов
❌ Лог может разрастись в огромный массив
❌ Нет отдельных файлов для specs, design, test-results
❌ Нет возможности хранить бинарные артефакты

**Для реальной работы используйте FILE_TASK!**

---

## Примечания для агента

Когда работаешь с hello-world:

1. **Всегда используй `TASK.*`**, не `HELLO_TASK.*`
2. **Резолвинг через project.yml** - обязательно
3. **Это тестовый режим** - можешь экспериментировать
4. **Сообщай о проблемах** - это поможет найти баги в фреймворке
5. **Следуй всем правилам CML** - они тестируются здесь

---

**Happy testing!** 🧪🚀

